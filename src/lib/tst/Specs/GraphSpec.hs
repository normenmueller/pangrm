module Specs.GraphSpec (spec) where

import Test.Hspec

spec :: Spec
spec = do
  describe "Internal Graph" $ do
    it "is not yet specified." $ do
      pendingWith "Graph tests need to be implemented!"

